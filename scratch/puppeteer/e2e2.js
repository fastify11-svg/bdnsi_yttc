const puppeteer = require('puppeteer'); const fs = require('fs'); const urls = ['/courses']; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); for (const url of urls) { console.log('Testing: ' + url); page.on('requestfailed', request => console.log('Failed:', request.url(), request.failure().errorText)); await page.goto('http://localhost:8000' + url, { waitUntil: 'networkidle2' }); } await browser.close(); })();  
