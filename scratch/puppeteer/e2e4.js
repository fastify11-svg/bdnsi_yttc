const puppeteer = require('puppeteer'); const fs = require('fs'); const urls = ['/']; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); for (const url of urls) { console.log('Testing: ' + url); let errors = []; page.on('pageerror', e => errors.push(e.message)); page.on('console', msg => { if(msg.type() === 'error') errors.push(msg.text()); }); await page.goto('http://localhost:8000' + url, { waitUntil: 'networkidle0' }); console.log(url + ' errors:', errors); await page.screenshot({ path: 'scratch/puppeteer/screen_' + url.replace('/', '') + '2.png' }); } await browser.close(); })();  
