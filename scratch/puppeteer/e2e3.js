const puppeteer = require('puppeteer'); const fs = require('fs'); const urls = ['/', '/all-course', '/verified-center', '/success-student', '/contact-us']; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); for (const url of urls) { console.log('Testing: ' + url); let errors = []; page.on('pageerror', e => errors.push(e.message)); page.on('console', msg => { if(msg.type() === 'error') errors.push(msg.text()); }); try { await page.goto('http://localhost:8000' + url, { waitUntil: 'networkidle2', timeout: 15000 }); } catch (e) { console.log('Timeout on ' + url); } console.log(url + ' errors:', errors); await page.screenshot({ path: 'scratch/puppeteer/screen_' + url.replace('/', '') + '.png' }); } await browser.close(); })();  
